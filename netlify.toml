# Netlify is no longer used — switched to GitHub Pages (free, no build credits).
# This file kept only to redirect anyone hitting the old icalmynt.netlify.app URLs.

[build]
  publish = "public"
  command = "echo 'No longer active'"

[[redirects]]
  from = "/*"
  to   = "https://mohamedmaged3002-droid.github.io/kixedo-ical/:splat"
  status = 301
